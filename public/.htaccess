# Disable MultiViews to prevent Apache from matching multiple content types
Options -MultiViews

# Enable the Rewrite Engine
RewriteEngine On

# Redirect www to non-www (HTTP or HTTPS) and keep URI/query string
RewriteCond %{HTTP_HOST} ^www\.fanalsabaghkuwait\.com$ [NC]
RewriteRule ^(.*)$ https://fanalsabaghkuwait.com/$1 [R=301,L,NE,QSA]
